import React from 'react';

export default function UserDetail(props){
 let person = Object.keys(props.user);
  //  console.log(Object.keys(props.user));
   
  return (  
    <div> 
        <div>{props.user.address}</div>)
    </div>)
        
        
          
  

} 